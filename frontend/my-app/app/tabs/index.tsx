import { Text, View} from 'react-native';
import React from 'react';

export default function Home() {
    return(
        <View className = "flex flex-grow bg-slate-200">
            <Text> Home page </Text>
        </View>
    );
}